name: Daily Settlement Automation

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'   # 9:00 AM Bangladesh Time (UTC 03:00)

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Google Chrome
        run: |
          sudo apt update
          sudo apt install -y google-chrome-stable

      # =========================
      # RUN CSV DOWNLOAD + DB UPDATE - COMMENTED OUT FOR TESTING
      # =========================
      # - name: Run day.py
      #   env:
      #     ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL }}
      #     ADMIN_PASSOWORD: ${{ secrets.ADMIN_PASSOWORD }}
      #     DATABASE_URL: ${{ secrets.DATABASE_URL }}
      #   run: |
      #     python day.py

      # =========================
      # RUN SETTLEMENT CREATOR
      # =========================
      - name: Run main.py
        env:
          COMPANY_EMAIL: ${{ secrets.COMPANY_EMAIL }}
          COMPANY_PASSWORD: ${{ secrets.COMPANY_PASSWORD }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python main.py
